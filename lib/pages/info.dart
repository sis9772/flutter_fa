// info.dart
import 'package:flutter/material.dart';

class InfoScreen extends StatelessWidget {
  const InfoScreen({super.key}); // const 생성자

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Info Screen'),
    );
  }
}
